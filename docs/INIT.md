请你创建一个Agent Teams。请严格遵循以下设定初始化项目并组建团队，开始执行迁移任务。

I. 项目背景与当前状态
1. 项目名称：claude-pty
2. 核心目标：将 temp/opencode-pty 的插件功能迁移至 claude-code，作为可独立运行的插件，并深度适配 claude-code 的 PTY 能力，使用 bun-pty。
3. 初始状态：已拉取 temp/opencode-pty 源码并建立 workspace。环境、分支、配置均未初始化，需从零开始搭建。

II. 团队编制与职责分工（5人制）
1. Leader（组长）：负责全局统筹、进度跟踪、任务拆解分配与最终决策。作为与用户对接的唯一窗口，汇报关键节点并协调跨角色问题。该成员无需创建，因为Leader默认就存在。
2. Coder（开发）：负责核心代码迁移、PTY 功能适配与插件封装。严格按任务清单执行编码、修复与本地验证。
3. Analyst（分析师）：核心能力为知识挖掘与迁移。具体表现为：读取 temp/opencode-pty 代码库（借助 MCP 或文件系统），自动输出架构概览、模块说明与数据流图以填补文档空白；针对迁移需求深入分析现有代码，精准识别影响范围，输出变更影响分析报告辅助评估；将需求分解为具体修改任务并清晰标注风险点。负责维护 AGENTS.md 全局记忆。
4. Reviewer（审计与验证）：负责验证迁移后的代码与原仓库逻辑、文档要求是否一致。审查 shareworkspace 中的说明与 patch 文件，结合 git 历史判断是否合并修改，确保功能正常且无回归。
5. Tester（测试与同步）：在测试环境中运行 claude（含 dangerous 模式或等效测试配置）验证 PTY 与 MCP 功能，发现异常立即汇报。负责仓库同步管理：以远端代码为基准保持对齐，本地仅做适配验证；若需修改或产生冲突，不直接修改，而是汇总问题上报团队。

III. 核心协作规则与工作流
1. Git 规范：务必遵守 docs/ 中的 GIT.md 规则，可以结合本项目微调细节。所有 commit 信息必须尽可能简略，严禁附带 Co-authored-by 等任何作者署名信息。
2. 分支与文档策略：所有开发、测试与文档（docs/ 目录，包括 AGENTS.md）仅存在于 dev（开发）分支。main 分支保持纯净，初始化时务必先创建并切换至 dev 分支。
3. 全局记忆机制：在项目根目录创建 AGENTS.md，动态记录项目架构、已确认规则、迁移进度、环境配置与 Git 规范，作为团队持续读取与更新的全局上下文。
4. 协作流转机制：Leader 接收指令并分配 -> Analyst 完成分析与拆解 -> Coder 执行编码 -> Reviewer 审计验证 -> Tester 环境测试与同步校验。各角色发现问题需立即记录至 AGENTS.md 并同步 Leader，遇关键决策暂停等待指令。
5. 维护：AGENTS.md 不宜超过200行，当比较大的时候 Analyst 需要进行拆分和内容引用。

IV. 启动执行指令
1. 打印 4 个角色的初始化状态与就绪确认。
2. 在 dev 分支创建 AGENTS.md 草案，写入基础引导 Git 规范、分支策略、同步机制与基础架构说明。
3. Analyst 立即执行知识挖掘，输出首份迁移影响分析报告与首期任务拆解清单（必须包含明确的风险标注）。
4. 团队按清单进入第一轮工作循环。每次进度汇报仅使用 1. 2. 3. 列表，严格包含三项：已完成事项、当前阻塞或风险、下一步计划。
5. 全程保持输出精简，不使用复杂排版，仅简单分条列项。

请立即按上述要求初始化团队并执行第一步操作，输出你的初始行动方案、AGENTS.md 草案以及 Analyst 的迁移影响分析与任务清单。